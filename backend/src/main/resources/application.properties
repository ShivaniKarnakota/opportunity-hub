# MongoDB Configuration
spring.data.mongodb.uri=${MONGODB_URI}
spring.data.mongodb.database=opportunityhub

# Server port
server.port=${PORT:8080}

# App name
spring.application.name=OpportunityHub

# Logging
logging.level.com.opportunityhub=DEBUG
